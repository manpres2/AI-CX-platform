# AIVoiceBotBFSI

Local Voice AI BFSI Demo — Whisper STT + LLaMA + Kokoro TTS + ChromaDB RAG

Built by Manpreet Singh | CPaaS Presales

## Changelog

Every change from here on gets a one-line entry below, plus its own commit/push, so any version can be recovered with `git log` + `git checkout <commit> -- <file>` or `git revert`.

- 2026-08-03 13:28 — Redesigned the bank admin panel to fill wide screens (fluid width + 2-column card layout) and refreshed it to an Apple-style dark theme — `static/admin.html`
- 2026-08-03 13:28 — Ported the same wide-screen/Apple-style redesign to the tech-support admin panel — `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-03 13:28 — Added a dark/light theme toggle (shared via localStorage) to both tech-support pages — `techsupport-voice-bot/static_tech/admin.html`, `techsupport-voice-bot/static_tech/index.html`
- 2026-08-03 13:28 — Added Hindi voice support (Kokoro Hindi voices, Whisper Hindi STT, Hindi LLM replies, localized canned lines) plus a Conversation Language selector in the admin panel — `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-03 13:28 — Switched the tech-support RAG embedding model to a multilingual one and rebuilt the KB index, so Hindi questions can retrieve the English knowledge base — `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/prompt_config_tech.json`
- 2026-08-03 13:28 — Added a `techsupport-voice-app` launch config entry for local previewing — `.claude/launch.json`
- 2026-08-03 13:28 — Attempted a latency fix (streamed sentence-by-sentence replies); reverted in full after it cut speech mid-sentence — `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/index.html`, `techsupport-voice-bot/prompt_config_tech.json`
- 2026-08-03 13:28 — Catch-up commit bundling all of the above (this session had been working uncommitted) — see files list in the commit itself
- 2026-08-03 21:06 — Added a "Call Transcripts" admin panel: full-call transcripts are now saved as a JSON sidecar per recording (reusing the Whisper transcription already run for the LLM prompt), with a searchable list, transcript viewer, playback, and delete — `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-03 21:11 — Added a "Delete All" option to the Call Transcripts panel (individual per-call delete already existed) — `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-03 21:47 — Replaced the dark "glass" theme (blur, gradients, glow shadows) on both admin panels with a flat, professional theme — solid colors, moderate corner radius, subtle shadows — and rebuilt the button tooltips as fully opaque with larger text/padding so they're clearly readable instead of blending into the background — `static/admin.html`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-03 21:58 — Swapped the admin panel accent/background/text colors for Kaleyra's real brand palette, extracted directly from kaleyra.io's live site (now hosted under tatacommunications.com) — accent `#F0424C` coral-red on dark theme, their own darker `#B60F17` shade for light theme (better contrast on white), background `#040A0E` — `static/admin.html`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-04 11:44 — Added cross-call caller memory to the tech-support bot: it now looks up the caller by name (matched by normalized spoken name, no phone caller ID available) and greets returning callers with a note about their last issue and whether it was resolved; each call's issue is summarized via the LLM and its resolved status inferred from farewell phrasing, then saved to a new per-caller history store, with a new "Callers" admin panel tab to view/delete that history — `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`, `.gitignore`
- 2026-08-04 12:42 — Added Phase 1 of the Local AI Meeting Intelligence Platform (new `meeting-intelligence/` app, port 8002): upload a recorded meeting → Whisper transcription (CPU) → pyannote speaker diarization (CPU, needs a one-time HF token — gracefully falls back to a single-speaker label until then) → Ollama/cloud LLM extracts tasks and decisions as JSON → SQLite storage + ChromaDB semantic search, with its own dashboard (Overview, Meetings, Search, LLM Settings) and its own independent, switchable local/cloud LLM config (default local model `gemma4:e4b`, separate from the two voice bots' LLM settings). Also added a new `portal/` app (port 8003) — a single unified dashboard aggregating live health/stats from all three apps plus one iframe tab per app for full drill-down management. Live Teams capture and outbound reminder sending are deferred to a later phase — see `E:\AI Voice bot\Live_meeting_capture.md` for the full design and phasing — `meeting-intelligence/`, `portal/`, `.claude/launch.json`, `.gitignore`, `requirements.txt`
- 2026-08-04 12:52 — Added the HF token and fixed a real dependency bug blocking speaker diarization: `pip install pyannote.audio` pulls in `speechbrain` 1.1.x by default, whose lazy-import system breaks pytorch-lightning's stack introspection (`ImportError: Lazy import of ... speechbrain.integrations.k2_fsa ... failed`) — pinned to `speechbrain==0.5.16` to fix it. Verified against a real two-speaker recording: pyannote now correctly separates and attributes every turn (previously fell back to a single-speaker label) — `requirements.txt`
- 2026-08-04 13:36 — Added a new `launcher/` app (port 8004): a single front-door page with four box-shaped tiles (Apex Bank Bot, TechCare Support Bot, Meeting Intelligence, Unified Ops Portal), each opening its app in a new browser tab — no auth, no backend logic, just a static launcher page — `launcher/`, `.claude/launch.json`
- 2026-08-04 13:40 — Restructured the launcher into a two-level tile grid: the two separate bot tiles were replaced with one "AI Voice Bots" tile that expands (in place, via a back link) into a sub-view with a tile per bot; both bot tiles now open that bot's `/admin` page instead of its public voice-agent page — `launcher/static_launcher/index.html`
- 2026-08-04 14:26 — Replaced single-shared-credential admin login with multi-user accounts + per-app permissions across all five apps: a new shared `users.db` (SQLite, gitignored, bcrypt-hashed passwords) backs a new `auth.py` duplicated into each app (`verify_admin`/`require_superadmin`, same `Depends()` signature as before so no route code changed), bootstrapped from the existing `ADMIN_USER`/`ADMIN_PASS` as a superadmin so nobody was locked out. Added a "Users" pane in the Portal (superadmin-only) to create/delete admin accounts and assign which apps each one can access — verified a restricted test user gets 200 on their permitted app and 403 everywhere else. Also made the launcher's logo/company name configurable (superadmin-only settings panel, public read) instead of hardcoded — `app/auth.py` (+4 duplicates), `app/main.py`, `techsupport-voice-bot/app/main_tech.py`, `meeting-intelligence/app/main_meet.py`, `portal/app/main_portal.py`, `portal/static_portal/dashboard.html`, `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `branding_launcher.json`, `.gitignore`
- 2026-08-04 14:41 — Added two more Meeting Intelligence features: (1) transcript upload — a new "Upload Transcript" control accepts an already-existing .txt/.vtt/.srt transcript and skips Whisper/diarization entirely, going straight to task/decision extraction (WebVTT `<v Speaker>` tags and "Name:" line prefixes are used for speaker attribution when present; refactored the shared post-transcription pipeline into `finish_processing()` so both the audio and transcript paths use the same code); verified against real .txt and .vtt samples, correctly extracting owners/deadlines/decisions. (2) Email/SMTP reminders — a new "Email Settings" pane (Gmail/Office 365 presets, generic SMTP-AUTH via stdlib `smtplib`) plus a "✉ Remind" button per open task that emails the resolved participant (matched by name, requires an email on file, now editable per participant); verified the SMTP connection genuinely reaches Gmail's real server and the no-email-on-file error path — `meeting-intelligence/app/main_meet.py`, `db.py`, `emailer.py` (new), `static_meet/admin.html`, `.gitignore`
- 2026-08-05 10:09 — Added `start_all.bat`/`stop_all.bat` at the repo root to launch/kill all five services with one script (staggers the two GPU-heavy voice bots 15s apart to avoid CUDA init contention on cold start — confirmed via testing that starting both at once can make the second one noticeably slower to come up). Also locked down the launcher (localhost:8004): the whole page now requires superadmin login instead of being public, added a live "Overview" status-tile section on the main page (reusing the Portal's health/sysinfo aggregation pattern) showing up/down + key stats for all four apps, and added a "Users" tile that deep-links straight into the Portal's Users pane (`#users` hash, which `dashboard.html` now reads on load) rather than duplicating user management on the launcher — `start_all.bat`, `stop_all.bat`, `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `portal/static_portal/dashboard.html`
- 2026-08-05 10:36 — Added dynamic bot provisioning: a new "Create New Bot" flow in the launcher's AI Voice Bots section provisions a genuinely new, independent voice bot — its own port (auto-assigned 8010-8099), its own detached `uvicorn` process, its own full admin panel — from a web form (name/slug/port/language/greeting/Whisper model), no code editing required. Built a genericized `bot-template/` (domain-neutral copy of the tech-support bot's app + static files; `app_key` for permissions is auto-derived from the bot's own folder/slug name) and a `bots_registry.json` (gitignored) that the Launcher and Portal both read fresh on every request, so a new bot becomes visible in tiles, Overview, and the Users permission list without restarting either service. Fixed a bug found during testing where the launcher spawned new bots without actually passing through the chosen Whisper model (`WHISPER_MODEL` env var was never set on the subprocess) — verified end-to-end: bot creation, folder scaffolding, RBAC scoping (a user granted only the new bot's permission gets 200 there, 403 everywhere else), start/stop lifecycle, and registry-based discovery by Portal/Launcher with no restart — `bot-template/` (new), `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `portal/app/main_portal.py`, `.gitignore`
- 2026-08-05 10:48 — Fixed launcher/platform navigation based on live usage feedback: tiles now navigate in the same browser tab instead of opening a new one (removed `target="_blank"` from every tile and the bot-tile "Admin" links); the launcher's internal view-switching (AI Voice Bots / Create New Bot sub-views) now pushes real browser history entries, so the Back button actually returns to the previous view instead of doing nothing; each of the other four apps' admin/dashboard header logo is now a clickable link back to the launcher (localhost:8004); and the AI Voice Bots tiles (built-in bank/tech bots plus any custom-provisioned ones) are now rendered from one unified, live-polled list showing whether each bot is actually up, its uptime (from its own `/api/sysinfo`), or its error if it's down — verified in-browser: same-tab tile navigation, working Back button with correct view restored, logo click-through back to the launcher, and live uptime/status on both built-in bot tiles — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `static/admin.html`, `techsupport-voice-bot/static_tech/admin.html`, `meeting-intelligence/static_meet/admin.html`, `portal/static_portal/dashboard.html`, `bot-template/static_bot/admin.html`
- 2026-08-05 11:02 — Redesigned the launcher's main page to match a reference SaaS dashboard layout the user supplied (a saved Kaleyra CPaaS "Channels" page — style/formatting only, none of its actual features were copied): a fixed left sidebar (nav links + branding settings), a top bar (page title, theme toggle, logged-in username), and a card-grid content area replacing the old centered hero + tile layout. Extracted the real design tokens by live-rendering the reference file and reading its computed CSS (not guessed): white/off-white light theme, `Heebo` typeface, blue `#1676EE` accent, soft green/red status tags, 8-10px card radii with a 1px hairline border and a bottom "Manage" link row — built a matching dark-theme palette alongside it since the launcher already had a theme toggle. The Overview and AI Voice Bots views now render through one shared `.card` component instead of two separate tile styles. Also had the server inject the logged-in superadmin's username into the page (`root()` now does a `%%USERNAME%%` substitution using the same `Depends(require_superadmin)` value already used to gate the route) so the top bar can show who's logged in — verified in-browser against the actual rendered design tokens (computed background/border/tag colors matched the reference exactly) plus theme toggle, Back button, and bot-status cards all still working — `launcher/static_launcher/index.html`, `launcher/app/main_launcher.py`
- 2026-08-05 11:11 — Added Stop/Start controls to the built-in bots (Apex Bank, TechCare) on the AI Voice Bots page — previously only custom-provisioned bots had these, built-in ones only had a "Manage" link. Stop reuses the existing netstat/taskkill-by-port helper (same mechanism `stop_all.bat` already uses); Start spawns a new detached `uvicorn` process with each bot's real launch command (`main:app --reload` for the bank bot, `main_tech:app` for the tech bot, matching `start.bat`/`start_tech.bat`), so it comes back up in whatever branding/prompt/KB state it was last saved in. Verified end-to-end: stopped TechCare from the UI (port 8001 confirmed down, tile flipped to a red "Down" tag with the connection error shown), then started it again from the same tile and confirmed `/health` came back with its correct config (`whisper: medium`, `cuda: true`) after model load — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`
- 2026-08-05 11:29 — Added an uploadable logo image to launcher branding as an alternative to the emoji (same upload/preview/remove pattern the bank bot's admin panel already uses for its own logo — PNG/JPG/SVG/WEBP/GIF, stored as `static_launcher/brand-logo.<ext>`, extension auto-detected on read). The sidebar brand mark and the settings panel both show the image in place of the emoji whenever one is uploaded. While building this I reproduced and fixed a real bug it exposed: bots started from the launcher's own Start button (built-in or custom) were still Windows child processes of the launcher, so tree-killing the launcher (`taskkill /T`, e.g. to restart it after a code change) took every bot it had started down with it too — confirmed by watching TechCare die as a side effect of restarting the launcher mid-session. Fixed by routing every spawn through `cmd /c start "" /B ...` so the immediate parent (a cmd.exe that exits right after launching) is gone by the time anyone tree-kills the launcher; re-verified by starting TechCare from the launcher, tree-killing the launcher's PID, and confirming TechCare stayed up and healthy throughout. Also gitignored `launcher/static_launcher/brand-logo.*` since it's uploaded content, not source — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `.gitignore`
- 2026-08-05 11:35 — Two small launcher polish fixes: the Branding settings panel now auto-closes ~900ms after a successful save (previously stayed open, showing "✅ Saved" indefinitely until manually dismissed), and the sidebar logo (emoji or uploaded image) is now 3x larger — verified in-browser: computed font-size went from 20.8px to 62.4px (exactly 3x) and the wider logo row still fits the 224px sidebar with room to spare — `launcher/static_launcher/index.html`
- 2026-08-05 20:22 — Added remote demo access: the whole platform's admin panels (bank/tech/meet/portal, plus any custom-provisioned bot) can now be reached through one tunnel to just the launcher. Built a generic reverse proxy (`GET/POST/PUT/PATCH/DELETE /proxy/{app_key}/{path}`) that forwards to the right backend and injects a small `window.__BASE_PATH__` script into HTML responses; every admin panel's frontend JS now reads that and prefixes its own absolute `fetch()`/`href`/`src` calls with it (a fully mechanical, ~130-reference sweep across bank/tech/meet/portal/bot-template found and confirmed by a dedicated research pass — every single one was an absolute leading-slash path). Portal's 3 embedded iframes and the launcher's own cross-app tile/nav links now go through `/proxy/...` too. Local direct-port access (`localhost:8000/admin` etc.) is completely unaffected — `BASE_PATH` is empty there, verified by confirming no proxy script gets injected outside the proxy path. Installed `ngrok` (downloaded directly, no package manager available on this machine) and added `start_demo_tunnel.bat`; the only remaining step is the user's own one-time free ngrok account signup (ngrok v3 requires an authtoken even for basic tunnels, can't be done on their behalf). Verified end-to-end: proxied health/API/HTML responses for all 4 apps, the nested portal→iframe→bot proxy chain, the logo "back to launcher" link, and the local-access regression check — `launcher/app/main_launcher.py`, `static/admin.html`, `techsupport-voice-bot/static_tech/admin.html`, `meeting-intelligence/static_meet/admin.html`, `portal/static_portal/dashboard.html`, `bot-template/static_bot/admin.html`, `launcher/static_launcher/index.html`, `start_demo_tunnel.bat` (new), `.gitignore`
- 2026-08-05 21:09 — Fixed `start_demo_tunnel.bat` silently showing no URL: it ran ngrok in the same console window with no `pause`, so if Windows intercepted the freshly-downloaded `ngrok.exe` with a SmartScreen/Firewall prompt (or ngrok exited for any other reason), the window just flashed and vanished before anything was visible. Rewrote it to launch ngrok in its own separate window, then poll ngrok's local API (`http://127.0.0.1:4040/api/tunnels`) from a PowerShell one-liner in the main window and print the public URL in plain text, with a troubleshooting message (and a final `pause`) if it can't be detected within 20 seconds — verified by running the actual script and confirming it printed the live URL, then confirming that URL really served the platform — `start_demo_tunnel.bat`
- 2026-08-05 21:27 — Added an Activity dashboard so it's visible whether anyone is actually opening the shared demo link: every page-level visit to the launcher (`/`) or through the reverse proxy (`/proxy/*`, filtered to HTML responses only — not every API call) is logged to a new `access_log.db` (SQLite, gitignored — holds real visitor IPs) with timestamp, IP, logged-in username, app, and path. Reads `X-Forwarded-For` first so visits through the ngrok tunnel show the real remote visitor's IP rather than the tunnel's own local address. New "Activity" pane on the launcher (summary cards for total visits / unique visitors / last-visit time, plus a recent-visits table) — verified end-to-end: hit the platform both directly on localhost and through the live public tunnel, confirmed the dashboard correctly showed the tunnel hit with its real external IP distinct from the local `127.0.0.1` entries — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `.gitignore`
- 2026-08-06 11:40 — Meeting Intelligence's "Upload Transcript" now accepts PDF and Word (.docx) files, not just .txt/.vtt/.srt — reused the already-installed `pymupdf`/`python-docx` libs (no new dependency) to extract text before handing it to the existing plain-text paragraph parser. Also closed a gap found while making this change: the upload endpoint previously had no server-side file-type check at all (only the HTML `accept` attribute, trivially bypassable) and would blindly try to UTF-8-decode any uploaded bytes; added an explicit extension whitelist on the backend. Verified by generating a real sample PDF and DOCX with pymupdf/python-docx and running them through the actual `parse_transcript_file()` in a live-restarted server process — both correctly extracted and speaker-segmented the text — `meeting-intelligence/app/main_meet.py`, `meeting-intelligence/static_meet/admin.html`
- 2026-08-06 11:58 — Added an animated indeterminate progress bar to the transcript upload card, shown for the full duration the server is parsing + running LLM task/decision extraction on an uploaded transcript (that request blocks until fully done, so there was previously no feedback beyond static "Uploading…" text for what can take a minute-plus). Upload area is also dimmed and disabled while in flight to prevent double-submits. Verified with a standalone mock-fetch harness (server sits behind HTTP Basic Auth, so it couldn't be driven end-to-end through the real browser login) confirming the bar shows/hides and the area disables/re-enables correctly across both the in-flight and completed states — `meeting-intelligence/static_meet/admin.html`
- 2026-08-06 12:35 — Fixed action-item owner attribution being wrong/unusable in Meeting Intelligence: the extraction prompt only said `"owner": "name or null"` with no guidance to distinguish who actually COMMITTED to a task vs. who was merely mentioned, and it saw raw diarization speaker labels (e.g. `SPEAKER_00`) rather than real names since owner renaming is an admin action that happens *after* extraction already ran — so a correct display name was structurally impossible before this fix. Rewrote the prompt with explicit commit-vs-mention guidance, a worked example, and a roster of the transcript's actual speaker names the model must pick from verbatim. Participants are now auto-named at insert time when their `speaker_label` is already a real name (true for most uploaded transcripts, e.g. "Alice:" prefixes) rather than a placeholder like `SPEAKER_00`/`Unknown`. Added a deterministic (non-LLM) email-address regex scan over each participant's own transcript segments to auto-fill their email when they state it themselves, without ever overwriting one the admin already entered. The meeting-detail and "tasks due" views now resolve and display each task's owner as the participant's real display name (falling back to the raw extracted string), and the "✉ Remind" button is disabled with an explanatory tooltip instead of appearing clickable and then failing when no email is on file — `meeting-intelligence/app/extraction.py`, `meeting-intelligence/app/db.py`, `meeting-intelligence/app/main_meet.py`, `meeting-intelligence/static_meet/admin.html`
- 2026-08-06 13:10 — Fixed "No decisions extracted" (and near-empty tasks) on a real 1h40m Teams meeting uploaded as a .docx transcript — root-caused by testing against the actual uploaded file rather than guessing. Two compounding bugs: (1) Microsoft Teams' "Download transcript" export shapes every turn as `Speaker   H:MM:SS\nutterance` (elapsed time, not a "Name:" line), and the existing colon-based speaker regex mistook the colon *inside* the timestamp (e.g. `0:17`) for the name delimiter, fragmenting the meeting's 6 real speakers into 105 fake ones (one per turn) — confirmed directly from the transcript sidecar JSON. (2) That meeting's transcript text ran ~92,000 chars (~23k tokens), but the local Ollama call never set `num_ctx`, so it silently ran at Ollama's small runtime default regardless of the model's real 131k context, truncating the prompt and producing malformed JSON (logged as "extraction parse error: {"). Added a dedicated Teams-turn-format parser (falls back cleanly to the existing generic parser when text doesn't match, verified against a plain "Name:" transcript) that also gives real per-turn timestamps instead of proportional dummies, and now sizes `num_ctx` to the actual prompt length (plus a `num_predict` floor so long outputs don't get cut short). Verified against the real uploaded file end-to-end post-fix: 105 fake speakers → 6 real ones, and a live extraction call against the running Ollama server produced real tasks with correct owners and a real decision (previously 0/0 with a parse error) — `meeting-intelligence/app/main_meet.py`, `meeting-intelligence/app/extraction.py`
- 2026-08-06 19:26 — Added an explicit Auto/CPU/GPU run-mode switch to Meeting Intelligence's LLM Settings (previously Gemma was silently force-CPU'd by a hardcoded name check, added earlier the same day to stop it fighting the two voice bots for the 3060 Ti's 8GB VRAM). Saving a changed setting now checks whether that model is currently loaded in Ollama and, if so, prompts the admin to restart it immediately (unloads via `keep_alive:0` so the new setting takes effect on the next request) or apply it later when the model naturally reloads. Verified end-to-end against the live Ollama server: `ollama ps` confirmed CPU/GPU split changes correctly, both the immediate-restart and apply-later paths, and that the prompt only appears when the model is actually loaded — `meeting-intelligence/app/extraction.py`, `meeting-intelligence/app/main_meet.py`, `meeting-intelligence/static_meet/admin.html`
- 2026-08-06 19:45 — Added a 167-page Solution Design Document for the whole platform (architecture diagram, per-app component breakdown, phased implementation roadmap, and a full Python source-code appendix for all 20 live application scripts) — generated with `python-docx` since Node/docx-js isn't installed on this machine, verified by rendering sample pages (cover, TOC, diagram, code listings) to JPEG via PyMuPDF and visually checking them — `docs/AI_CX_Platform_Solution_Design_Document.docx` (new, gitignored-scale asset kept in repo), `E:\...\scratchpad\sdd\diagram.py`/`build_sdd.py` (build scripts, not committed).
- 2026-08-06 20:11 — Two platform-wide additions. (1) Start/Stop control (previously only the two built-in voice bots had it) extended to Meeting Intelligence and Unified Ops Portal directly from the Launcher's main Overview page, so the admin can free up GPU/VRAM by turning off whatever isn't currently needed — reuses the existing netstat/taskkill + detached-uvicorn-spawn mechanism the bot tiles already used. (2) Added a new **AI Studio** app (port 8005): a "GPT AI Chat" tile (multi-turn chat against a local Ollama model or a cloud OpenAI-compatible endpoint, each with its own model + Auto/CPU/GPU picker, same pattern as Meeting Intelligence) and an "Agentic AI" tile that can read/write files and run shell commands on the admin's behalf once explicitly delegated (off by default). Every agent action is classified safe (auto-run) / risky (queued for one-click admin approval) / blocked (hard-refused regardless of approval — pattern-matches destructive commands like `format`, `shutdown`, `rm -rf /`, disabling security tooling, or piping a download straight into a shell) and written to a full audit log; file tools are additionally sandboxed to a configurable workspace root with path-traversal protection. Wired into the Portal's iframe tabs and Users/RBAC list, and the Launcher's tile grid/Start-Stop/health-polling, under the new `studio` app key. Verified live end-to-end: Chat got a real reply from `llama3.1:8b`; a safe `list_dir` auto-ran; a `write_file` correctly paused for approval, wrote nothing until approved, then wrote the exact requested content; denying a second write left the filesystem untouched and redirected the model; workspace path-escape attempts (`../`, absolute paths outside the workspace) were refused; and the classifier was unit-tested against 14 representative safe/risky/blocked commands (all correct). Also fixed a real bug found during this testing: the local model doesn't always nest tool arguments under `"args"` as instructed, which was silently executing tool calls with empty args — now falls back to treating flattened top-level fields as the args instead — `ai-studio/` (new: `app/main_studio.py`, `app/agent.py`, `app/llm.py`, `app/auth.py`, `static_studio/admin.html`, `start_studio.bat`, `stop_studio.bat`), `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `portal/app/main_portal.py`, `portal/static_portal/dashboard.html`, `start_all.bat`, `stop_all.bat`, `.claude/launch.json`, `.gitignore`
- 2026-08-06 20:16 — Fixed the Launcher's Meeting Intelligence/Portal/AI Studio tiles only opening on "Manage" instead of anywhere on the tile — making the whole tile clickable (matching the AI Voice Bots tile) had been left out when Start/Stop buttons were added to those tiles earlier the same day (a `<button>` can't sit inside an `<a>`, so the card was changed to a plain `div` and the click-to-navigate behavior was dropped along with it). Restored via an `onclick` on the card itself, with `event.stopPropagation()` on the Start/Stop control so clicking Stop/Start no longer also navigates away. Verified live: clicking a tile body now opens its `/proxy/{app}/admin` page, and clicking Stop/Start on a tile stays on the Overview page and doesn't trigger navigation — `launcher/static_launcher/index.html`
- 2026-08-06 20:29 — Fixed `stop_all.bat` leaving ~3.5GB of VRAM in use after running it — every app's own process was correctly killed (ports 8000-8005 all confirmed down), but Ollama (port 11434) runs as its own background server independent of those six, and kept whatever model was last used loaded in GPU memory regardless. `start_all.bat` now starts Ollama first if it isn't already running (checked via the same netstat pattern used everywhere else in this file), and `stop_all.bat` now includes port 11434 in its existing kill loop, so "stop everything" actually frees everything. Verified end-to-end: confirmed Ollama wasn't running, ran the start logic and got a real response from its API; ran the stop logic and confirmed both the port closed and GPU memory usage dropped (3794MiB → 607MiB) — `start_all.bat`, `stop_all.bat`
- 2026-08-07 10:09 — Added Veena (Modal) as a third cloud TTS engine option, alongside ElevenLabs/OpenAI, admin-switchable from Voice & Model → Cloud TTS Engine. Includes a standalone Modal deployment script (`modal deploy modal_veena_tts.py`) that loads maya-research/veena-tts (4-bit quantized, Llama-based, Hindi/English) + the SNAC 24kHz decoder and serves it as an HTTP endpoint returning raw PCM matching the bots' existing `SAMPLE_RATE=24000` (no resampling needed); optional shared-secret auth via a Modal secret. Applied to both the live bank bot and the bot-template used for future "Create New Bot" instances — `modal_veena_tts.py` (new), `bot-template/modal_veena_tts.py` (new), `app/main.py`, `bot-template/app/main_bot.py`, `static/admin.html`, `bot-template/static_bot/admin.html`
- 2026-08-07 11:01 — Extended the same Veena (Modal) cloud TTS option to the tech-support voice bot (the third and last app with this pluggable provider pattern — `bots/` has no created bots yet, so bot-template already covers future ones). All three admin panels and their own copy of the deploy script now share the same Modal app name (`veena-tts`), so a single `modal deploy` from any one of them creates one endpoint usable by all — `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`, `techsupport-voice-bot/modal_veena_tts.py` (new)
- 2026-08-10 11:29 — Added a "Download a model" card to AI Studio's GPT AI Chat tile: type a model name (or paste a full `ollama pull …` command) and click Download to pull it from Ollama, with a live progress bar/status; once it finishes the model is auto-selected in the existing Model dropdown, ready to test in chat. Implemented as a background `asyncio` task on the server (not a raw streaming HTTP response) plus 1.2s-interval polling from the browser, because the Launcher's `/proxy/{app}` reverse proxy fully buffers responses with a 60s timeout — a long-lived streaming pull would silently break for anyone using the platform through that tunnel. Verified live end-to-end: started Ollama, pulled a real model (`smollm2:135m`) through the new UI, watched the progress bar/percentage update in real time, confirmed it appeared in the Model dropdown afterward, and got a real chat reply back from it — `ai-studio/app/llm.py`, `ai-studio/app/main_studio.py`, `ai-studio/static_studio/admin.html`
- 2026-08-10 12:01 — Made the Launcher's AI Voice Bots tiles fully clickable (previously only the "Manage" text link opened a bot's admin panel; the rest of the tile did nothing) and added a second "💬 Chat" link per tile that opens that bot's live voice-agent page (`/proxy/{slug}/`) instead of its admin panel (`/proxy/{slug}/admin`) — matches the click-to-navigate + `event.stopPropagation()`-guarded controls pattern already used by the other Overview cards. Applies automatically to every bot, built-in or custom-provisioned, since the tile list is rendered generically from `/admin/api/bots` — no `bot-template/` changes needed, it has no tile-rendering code of its own. Verified live: clicking a tile body opened TechCare's admin panel, clicking its "💬 Chat" link opened its live voice-agent page instead, and Start/Stop buttons still work without triggering tile navigation — `launcher/static_launcher/index.html`
- 2026-08-10 12:39 — Renamed "AI Voice Bots" to "AI Bots" platform-wide and added a second bot type: text **Chat** bots, alongside the existing **Voice** ones, chosen once via a new Bot Type selector in "Create New Bot" (`launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`). A chat bot reuses the same bot-template as voice bots — same KB/RAG, guardrails, prompt config, caller-name memory, farewell detection, transcript logging, LLM provider settings — but skips everything audio-specific: no Whisper/Kokoro model loading at startup (saves real GPU/VRAM — confirmed via a live-created test bot's own log line), no mic/waveform UI (a new typed-message chat UI reusing the existing `.msg`/`.msg-bubble` styling instead), and the admin panel hides the TTS voice picker, Background Audio, and raw Recording Logs panels for that bot (`bot-template/app/main_bot.py`, `bot-template/static_bot/index.html`, `bot-template/static_bot/admin.html`). Also fixed a real bug this surfaced: the admin Branding-save endpoint did a blind overwrite instead of a merge, so saving a tagline/logo change would have silently wiped the new `kind` field back to the "voice" default on the very next save — now merges. Also fixed a latent XSS gap in the voice transcript renderer (raw text was interpolated into `innerHTML` unescaped) while adding the same renderer's chat counterpart. Verified live end-to-end: created a real chat bot, confirmed Whisper/Kokoro were skipped in its own log, sent it a message and got a real LLM reply with caller-name pickup, confirmed voice-only admin panels were hidden, and confirmed the branding-merge fix preserves `kind` across a save — then deleted the test bot. Also updated `start_all.bat` so Apex Bank Bot and Tech Support Bot are no longer auto-started with the rest of the platform (per explicit request — bots are enabled one at a time from the Launcher's "AI Bots" panel instead); `stop_all.bat` needed no change since it already stops by port regardless of who started them — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `bot-template/app/main_bot.py`, `bot-template/static_bot/index.html`, `bot-template/static_bot/admin.html`, `start_all.bat`
- 2026-08-10 13:02 — AI Studio's GPT Chat can now read and write documents. **In:** a new 📎 attach control accepts PDF, Word (.docx), text (.txt/.md/.csv) and image files — PDFs/Word are extracted to plain text server-side (reusing the same PyMuPDF table-aware + python-docx extraction the voice bots already use for their knowledge base, so no new dependency) and folded into the message the model sees, while images are passed as base64 to the model's vision input. Since Ollama silently ignores images sent to a non-vision model (which looks exactly like the model hallucinating about a picture it never saw), the upload endpoint checks the selected model's advertised capabilities via `/api/show` first and the UI flags the attachment with a "model can't see images" warning instead — pair it with the model-download box added earlier to pull e.g. `llava:7b`. Note the repo has `pytesseract` installed but no Tesseract binary on PATH, so OCR of scanned documents isn't available; vision models are the path for those. **Out:** every assistant reply gets ⬇ PDF / Word / Text buttons that render its (loosely markdown) content into a real downloadable document — PDFs via PyMuPDF's Story API, Word via python-docx with **bold**/*italic*/`code` converted to genuine Word runs rather than left as literal markers, both handling headings, bullet and numbered lists. Model output is HTML-escaped before the small set of supported inline tags is re-introduced, so a reply containing raw HTML can't inject markup into the generated PDF. Verified live end-to-end: uploaded real PDF/DOCX/TXT fixtures and confirmed the extracted text round-trips, confirmed an unsupported file type (.bat) is rejected server-side, confirmed vision detection correctly reports `false` for `llama3.1:8b`, asked a question about an attached policy document and got a correct answer quoting it, and exported a reply to PDF/DOCX/TXT confirming correct magic bytes, MIME types and filenames — `ai-studio/app/documents.py` (new), `ai-studio/app/llm.py`, `ai-studio/app/main_studio.py`, `ai-studio/static_studio/admin.html`
- 2026-08-10 13:40 — Fixed the "Create New Bot" flow leaving the admin staring at nothing: on success it now jumps straight to the AI Bots list and shows the new bot immediately as a greyed-out, non-clickable tile with a **real** startup progress bar, instead of staying on the form while the bot booted invisibly (and then appearing as a misleading red "Down" tile until the page was manually revisited). Progress is genuine rather than a guessed timer — a new `/admin/api/bots/{slug}/startup` endpoint reads that bot's own `logs/server.log` and maps the milestones it already writes (Whisper loaded → speech synthesis → knowledge-base index) to a percentage, scoping to the last startup marker in the file so a *restarted* bot reports the new run's progress rather than the previous run's completion. The tile flips to the normal Live state automatically the moment `/health` responds, with a toast; the same treatment now applies to the Start button on any existing bot (built-in or custom), which previously also just sat there during the ~45s Whisper/Kokoro load. Chat bots get their own wording ("Preparing…") since they load no speech models at all. Verified live: watched a real voice-bot start go through the greyed state (opacity 0.6, pointer-events none, amber pulsing "Starting" tag, bar at a log-derived 82% with stage text) and then flip to Live/clickable with Manage + Talk links restored; also unit-checked the log parser across every milestone plus the multi-restart case. Also fixed custom chat bots showing the 🤖 voice icon instead of 💬 (the launcher hardcoded the icon for registry bots), and gitignored the per-bot `start_<slug>.bat`/`stop_<slug>.bat` scripts the create flow generates — machine-specific artifacts like `bots/` itself — using an ignore-all-then-re-include-ours pattern so a future bot slug can't collide with the platform's own scripts — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `.gitignore`
- 2026-08-10 13:57 — Fixed the bot tiles' "Chat"/"Talk" links opening a page with no chat window (and, for voice bots, a mic that could never connect). Both linked through the Launcher's `/proxy/{slug}/` reverse proxy, but the public agent page — unlike every admin panel — was never part of the 2026-08-05 `window.__BASE_PATH__` sweep, so its `fetch('/api/branding')` resolved against the launcher instead of the bot and failed; the page then fell back to its default "voice" layout, which is why a chat bot showed no text input at all. Even with that fixed the page still couldn't work proxied: the live agent talks over a WebSocket and the proxy route only forwards GET/POST/PUT/PATCH/DELETE, with no websocket handler, so the connection would silently never open. Both links now point directly at the bot's own port (`http://localhost:<port>/`) and open in a new tab (also requested), leaving "Manage" on the proxy since admin panels do honor BASE_PATH and are plain HTTP. Note this means Chat/Talk are local-only — they were already non-functional through the ngrok tunnel for the WebSocket reason above, so this is a fix rather than a regression; making them work remotely would require adding WebSocket relaying to the launcher proxy. Verified live: tile links now carry the direct port and `target="_blank"`, the chat page loads its chat UI with the WebSocket OPEN, and a typed message round-tripped end-to-end (bot picked up the sender's name and answered in context) — `launcher/static_launcher/index.html`
- 2026-08-10 21:44 — Fixed Meeting Intelligence extracting **zero** tasks and decisions from every uploaded transcript. The transcript itself, participants and search index were all fine — the LLM extraction call was failing outright and the failure was invisible in three separate ways. Root cause: extraction sent the whole meeting as one prompt, and a 100-minute meeting is ~88k characters, which the `num_ctx` sizing turned into a ~32k-token context window; `llama3.1:8b` weights plus a KV cache that size come to ~9GB, so with the admin's `gpu_mode: gpu` (`num_gpu: 999`) forcing full offload onto an 8GB RTX 3060 Ti the runner overflowed into shared system memory and prompt evaluation crawled — confirmed live via `/api/ps` reporting `size_vram` 8.96GB on an 8GB card, and by the same call succeeding only when given a 1500s timeout, versus the code's 300s. Extraction now runs over ~12k-character windows (split on segment boundaries, 6 lines of overlap so a task raised at the end of one window and accepted at the start of the next survives), merging and de-duplicating the results — same meeting now takes **121s instead of timing out**, yielding 23 tasks and 20 decisions. Fixed two things this surfaced: (1) the prompt's own worked example was being regurgitated as a real task — "Update the pricing page" appeared in the output though "pricing" occurs 0 times in the transcript — now labelled as illustrative-only in the prompt and filtered on the way out, which matters far more once one prompt becomes fifteen; (2) owners the model invented are nulled unless they match an actual transcript speaker, keeping the task but dropping the fabricated name. Also closed the visibility gap: httpx timeout/read errors stringify to empty, so every failure logged as a bare `LLM call failed:` with no cause — the exception class name is now always kept; and a meeting whose extraction failed was still marked `done` with no error recorded, making it indistinguishable from a meeting that genuinely contained no tasks, so the reason is now stored against the meeting (cleared on a successful re-run) and shown as a warning banner above the transcript in the admin detail view — `meeting-intelligence/app/extraction.py`, `meeting-intelligence/app/main_meet.py`, `meeting-intelligence/static_meet/admin.html`
- 2026-08-10 21:53 — Added a **Logging** tab to the Launcher's main page: every service's log file in one place, one tab per service — Launcher, Meeting Intelligence, Unified Ops Portal, AI Studio, Apex Bank Bot, TechCare Support Bot, and one tab per provisioned custom bot (read from the bot registry, so a newly created bot gets its own tab automatically). Each tab carries the full option set: minimum-level filter (all/debug/info/warnings/errors), how many recent lines to read (100–5000), a free-text filter, 5-second auto-refresh, follow-tail, plus Refresh / Copy / Download / Clear. Log lines are parsed into timestamp + level + message and colour-coded, with a continuation line (a traceback body, which carries no `[LEVEL]` of its own) inheriting the level above it — so filtering to "errors only" returns whole tracebacks rather than just their first line. The view opens on whichever service wrote most recently, which after a failure is almost always the one worth reading. Only the last 2MB of a file is ever read and the tail is taken before filtering, so these ever-growing files can't be pulled into memory whole. Service keys are looked up in a fixed table and never joined onto a path — verified a `../../.env` key returns 404, alongside 404s for unknown services. This surfaced that three services had no log file at all to show: **AI Studio had no logging whatsoever**, and the **Portal** and the **Launcher** logged to the console only, so their history died with the terminal window — all three now write the same file+console log every other app already used, each with a startup marker so restarts are visible and a healthy-but-quiet service doesn't show an empty tab. The Launcher additionally now logs its lifecycle actions (bot and app start/stop, bot create/remove, log clear) with the username that triggered them — it is the one component that starts and stops everything else and previously kept no record of it. Also fixed a real bug this uncovered: `toast()` was called by the bot-provisioning poller but **never defined anywhere in the launcher**, so every "<bot> is live" notification threw a ReferenceError that landed in the poller's own catch block and was silently swallowed — the function now exists, which is also what the new Clear/Copy actions report through. Clear truncates rather than deletes, since the running service holds an open handle and deleting the file out from under it would leave it logging into nowhere until its next restart; it asks for confirmation and points at Download first. Verified end-to-end against the real app (auth dependency overridden on a scratch port so no credentials were handled): all 7 tabs listed and populated, level filter correctly pulling whole Whisper tracebacks out of the Meeting Intelligence log, text search matching 208 lines in the 316KB bank-bot log, empty-state and no-match states, traversal and unknown-key 404s, download filename/Content-Type, and no console errors — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `portal/app/main_portal.py`, `ai-studio/app/main_studio.py`, `.gitignore`
- 2026-08-18 19:21 — Swapped the machine's GPU from an RTX 3060 Ti to an RTX 3070 Ti (8GB). Verified no functional change is needed anywhere on the platform: both cards are Ampere with compute capability 8.6, and the installed PyTorch (2.5.1+cu121) already ships `sm_86` kernels, so `torch.cuda.is_available()` is `True` and the new card is detected correctly with no torch/CUDA/Ollama reinstall. VRAM is identical at 8GB, so every capacity decision made against that ceiling still holds unchanged — Gemma force-CPU'd by default in extraction, diarization defaulting to CPU, the ~12k-character extraction windows that stopped `llama3.1:8b` overflowing into shared memory, chat bots skipping Whisper/Kokoro, and `stop_all.bat` killing Ollama on 11434 to free the card. No source hardcodes a GPU model, VRAM size or PCI ID — every device decision is `torch.cuda.is_available()` or Ollama's own `num_gpu` — so the only stale thing was documentation: two comments explaining the (still accurate) 8GB constraint named the old card. Updated both; the dated README entries above are left as-is since they record what was true at the time — `meeting-intelligence/app/diarization.py`, `meeting-intelligence/app/extraction.py`
- 2026-08-19 17:45 — Added **RAG-first (grounded) answering** to every voice and chat bot, on by default, with a toggle and a relevance floor in each admin panel's System Prompt tab. Previously the knowledge base was only ever *optional background*: whatever the top-K search returned got pasted into the prompt and the model was free to answer from its own training instead — and because nothing was filtered, an unrelated chunk was injected with exactly the same authority as a bulls-eye match. Now the knowledge base is consulted first on every turn, only chunks clearing the relevance floor are used, and the model is told they outrank anything it thinks it knows; **only** when nothing clears the floor is it allowed to answer from general knowledge, and it is then instructed to say that's what it's doing rather than passing it off as documented material. Relevance is computed as cosine similarity against the returned vectors rather than read off Chroma's own distance — the collections are built in Chroma's default squared-L2 space over un-normalised embeddings, so its distances have no fixed 0–1 range to threshold against (which also means the admin panel's old "Relative Confidence: 1-distance" number was not a similarity at all; it now shows the same cosine score the live turn uses, and dims chunks that fall below the floor as "ignored"). Every decision is logged (`RAG-first ▶ 2 of 3 chunk(s) cleared relevance 0.35 (best 0.61, from home_loans.txt) — answering from the knowledge base`), so the Launcher's Logging tab shows exactly why any given answer was grounded or not. Defaults to on at 0.35 for existing bots too, since the config is read with fallbacks rather than requiring a migration. The bank bot keeps its verified-customer record as separate authoritative context and takes the grounding rule alongside it. Verified in the browser against the running tech-support panel: no JS errors, the toggle renders as a full-width card in the System Prompt tab, defaults to on at 0.35 when the keys are absent, and dims the floor input when switched off. Existing scaffolded bots under `bots/` were re-copied from the template, which is how they receive code changes — `bot-template/app/main_bot.py`, `bot-template/static_bot/admin.html`, `app/main.py`, `static/admin.html`, `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`, `prompt_config.json`, `techsupport-voice-bot/prompt_config_tech.json`
- 2026-08-19 17:53 — Made **online (cloud) LLMs a pick-from-a-list choice** in every place the platform lets you select a model: both voice bots, the bot template (so every bot scaffolded from it), Meeting Intelligence's extraction model, and AI Studio's Chat *and* Agent sections. The Cloud API option previously offered a bare Base URL box, so using anything other than OpenAI meant knowing the provider's endpoint *and* its exact model ids by heart. There is now an **Online LLM Provider** dropdown — NVIDIA NIM, OpenAI, Anthropic, Groq, OpenRouter, Together AI, DeepSeek, Mistral AI, or Custom — which fills in the endpoint, sets the API-key placeholder, links to that provider's key page, and offers its common model ids in the Model box (still free text, so any id works). Since a hard-coded model list goes stale, a **Load models** button asks the provider itself what it currently serves via its `/v1/models` endpoint and populates the dropdown from the live answer — falling back to the saved API key when the key box is left blank, exactly like the existing Test button. This works because every one of these providers speaks the same OpenAI-compatible `/chat/completions` API the platform already used; no call-path code changed, only what fills the config. Endpoints were verified by probing each one rather than trusted from memory (NVIDIA's returns a 404-shaped body for a missing auth header, which is why it needs checking rather than assuming), and the NIM model suggestions are taken from its live catalogue. Picking a provider never overwrites a model id typed by hand — only one that came from another preset. Verified: the new endpoint returns all 102 NIM models end-to-end through the real AI Studio route (with the admin dependency overridden so no credentials were handled), rejects an unknown section with a 400, and surfaces a bad key as a readable error; in the browser, all three panel shapes sync the dropdown to whatever endpoint is saved, fill the URL/model/placeholder/hint on switch, keep hand-typed models, and report a service still running an older build as "restart it" rather than as a provider fault. **Restart a service to pick up its new Load models endpoint** — the presets themselves work immediately, since the admin panel HTML is read from disk per request — `bot-template/app/main_bot.py`, `bot-template/static_bot/admin.html`, `app/main.py`, `static/admin.html`, `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`, `meeting-intelligence/app/main_meet.py`, `meeting-intelligence/static_meet/admin.html`, `ai-studio/app/main_studio.py`, `ai-studio/app/llm.py`, `ai-studio/static_studio/admin.html`
- 2026-08-19 18:05 — Moved the RAG-first controls from the **Prompt Config** tab to the **Knowledge Base** tab, where they belong — they govern how the knowledge base is used, not how the prompt is worded — and gave them their own Save/Reload row rather than sharing the Prompt Config tab's. The two keys still live in the same `prompt_config.json`, so saving from the Knowledge Base tab does a read-modify-write of the stored config instead of posting an object rebuilt from a pane the admin may never have opened (which would have wiped the system prompt); the Prompt Config tab's own save still writes both keys, so it can't drop them either. Switching to the Knowledge Base tab refreshes just these two fields, leaving any unsaved prompt edits alone. Verified in the browser with the API stubbed: loads the stored values, dims the relevance floor when switched off, and a save posts only the two changed keys with system prompt, greeting, guardrails, kb_filter and rag_top_k all intact — `bot-template/static_bot/admin.html`, `static/admin.html`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-19 18:31 — Gave the whole platform **one visual identity**: the Launcher's blue, now on all ten pages. There were three competing looks — the Launcher was blue on Heebo, the five admin panels were red on Inter, and the customer-facing agent pages were Apple-blue with 24px corners — so the same product looked like three products depending on which port you were on. Every page now carries a **byte-identical** canonical token block (verified by hashing it out of all ten files): blue `#1676EE` light / `#4A9CFF` dark, Heebo throughout, one 6/8/10/12px radius scale, and one set of semantic colours. Freeing red from being the brand colour also fixed a real legibility problem the mockups exposed — a red primary button sat next to a red "Down" pill, so the accent and the error state were indistinguishable; red now only ever means something is wrong. The Launcher's `--success`/`--danger` were folded into the `--green`/`--red` names the other nine pages use, kept as `var()` aliases so no existing rule silently lost its colour (verified both resolve correctly in both themes). Theme handling was normalised too: every page is now light-first, boots from one shared `aicx-theme` storage key instead of five per-app keys, and the **bank bot's admin panel and agent page — the only two pages in the platform with no light/dark switch at all — gained one**, which meant first converting the bank agent page off the hardcoded slate palette it predated the design system with (28 literal colours that would have rendered dark cards on a light ground) onto tokens. Agent pages keep their translucent panels deliberately, since those float over a gradient wash and would flatten into nothing as solids — they take the same hue, type, radii and status colours as everything else, with only the see-through surfaces overridden in a clearly-marked addendum. Verified page by page in the browser: the token pair resolves to the right values in both themes on Launcher, Portal, AI Studio, Meeting Intelligence, both bank pages and the tech bot; Heebo actually loads (which caught the bank agent page still naming a raw system stack, and the Launcher's four bare `monospace` declarations); the bank admin panel's brand-new toggle flips, relabels and persists; and a static audit confirms no page has a colour declaration left that doesn't read a token, bar white-on-accent button text and the mic button's semantic red/green — `launcher/static_launcher/index.html`, `portal/static_portal/dashboard.html`, `ai-studio/static_studio/admin.html`, `meeting-intelligence/static_meet/admin.html`, `static/admin.html`, `static/index.html`, `techsupport-voice-bot/static_tech/admin.html`, `techsupport-voice-bot/static_tech/index.html`, `bot-template/static_bot/admin.html`, `bot-template/static_bot/index.html`
- 2026-08-19 18:43 — Made the **AI Bots panel paint instantly** instead of sitting empty for seconds. `loadBots()` awaited `/admin/api/bots`, and that endpoint probed every bot's `/health` **one after another** before returning anything — so nothing at all rendered until the slowest probe finished. Measured the actual cost rather than assuming: a bot that is **down** takes ~2.3s to refuse the connection on this machine (a closed local port is not refused instantly here), so the wait scaled with the number of stopped bots — four of them cost 8.4s of dead air. Three changes. (1) The listing is now built from the registry alone and the page asks for it with `?status=false`, which touches no network and returns in **9ms**, so tiles — name, icon, port, Manage/Talk links — are on screen immediately, each carrying a pulsing grey "Checking…" tag and a disabled power button. (2) A new `/admin/api/bots/status` probes every bot **concurrently** and the page patches each tile in place when it lands: the tag becomes Live/Down, the caption becomes the uptime or the error, and the button becomes Start or Stop. Concurrency is what keeps that second phase flat as bots are added — the same four stopped bots went from 8.44s to 2.41s, 3.5x faster. (3) Probes now go to `127.0.0.1` rather than `localhost`, since resolving the name cost real time on every check — an **up** bot answers in 7ms on the literal address against 267ms on the name, a 38x difference; the displayed URLs are unchanged. The Overview card keeps using the combined endpoint, which is now concurrent too. Verified end-to-end against the real app with the superadmin dependency overridden so no credentials were handled (list 0.009s with no status field, status call returns every slug, combined call still works), and in the browser with the endpoints stubbed: tiles painted in **1ms** showing "Checking…", then flipped to Down/"connection refused"/Start and Live/"up 4h 12m"/Stop once status arrived, with no console errors — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`
- 2026-08-19 18:57 — Added **switchable speech engines** to every bot — both voice bots, the template and so every bot scaffolded from it. Speech-to-text previously had no admin control at all: the Whisper size was fixed by a `WHISPER_MODEL` env var read once at import, so trying a different one meant editing the environment and restarting. The Voice & LLM tab now carries a **Speech-to-Text Engine** card: pick any of Whisper's 14 models with its size and a ✓ against the ones already on disk, **⬇ Download** pulls the weights on demand with a live progress bar (whisper's own downloader, so the checksum is still verified, while the status endpoint watches the file grow), and **⚡ Use This Model** swaps it into the running bot. The swap is deliberately safe: the new model loads on a worker thread and only becomes the live one once it is fully ready, so a call in progress keeps transcribing on the old model rather than hitting a half-loaded one — verified by asserting the served model object is unchanged while the load is in flight, then changed once it completes. The choice is saved to `runtime_config.json` and survives a restart. Alongside it, an STT **Cloud API** mode sends audio to any OpenAI-compatible `/audio/transcriptions` endpoint (OpenAI's `whisper-1`, Groq's `whisper-large-v3`), which is worth having when the GPU is busy; if that call fails the bot falls back to the local model rather than dropping the caller's turn. TTS engine switching already existed and stays where it is, but the OpenAI TTS model was hardcoded to `tts-1` and is now configurable too. Two bugs surfaced and were fixed on the way: saving the Voice & LLM tab rebuilt `runtime_config.json` from scratch and so would have **silently wiped the STT model** chosen on another pane (now a read-modify-write); and `loadProviders()` threw on the very first line for **chat** bots, because their admin panel removes the TTS mode row it assumed was there — meaning a chat bot's cloud-LLM settings had never populated at all, which is why this went unnoticed. Verified against the real app with the admin dependency overridden: all 14 models listed with the 3 already cached correctly marked, bogus and un-downloaded models refused with a 400, the API key masked on read and preserved when the field is left blank; in the browser, a genuine 75MB download ran 33% → 100% and flipped the option to "✓ downloaded"; and a real voice-bot swap from `tiny` to `base` completed with `transcribe()` then running through the new model — `bot-template/app/main_bot.py`, `bot-template/static_bot/admin.html`, `app/main.py`, `static/admin.html`, `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-19 19:04 — Follow-up to the speech-engine work, fixing two things that made it look broken. **(1) The Whisper dropdown sat empty with no explanation.** `loadSttModels()` bailed silently on a non-OK response, and a bot still running the previous build answers 404 for the new endpoints — so the model list, the selected model and the status line were all blank with nothing saying why. It now says "Restart this bot to enable model switching (it is running an older build)" for a 404, and reports the status code for anything else. **(2) TTS had no model to choose at all** — only an engine (Kokoro vs cloud) and a voice. Local Kokoro now exposes its weights repo as a **TTS Model** field with the released repos suggested (`hexgrad/Kokoro-82M`, `hexgrad/Kokoro-82M-v1.1-zh`, both confirmed live on Hugging Face) and free text for any future one, which is the point — new models can be tried as they appear without a code change. Saving a different repo downloads it and rebuilds the voice pipeline on a background thread, so the next spoken line uses it with no restart; the default is now named explicitly rather than left to kokoro's fallback, which also silences its "Defaulting repo_id" warning on every boot. The cloud side had a matching gap: the previous commit made the OpenAI TTS model configurable in the backend but never added the input, so it was stuck on `tts-1` — there is now a Model field offering `tts-1`, `tts-1-hd` and `gpt-4o-mini-tts`. Verified against a scratch bot: all 14 Whisper models list with the configured one selected, both TTS model fields populate from the saved config with their suggestion lists, the stale-build message renders when the endpoint 404s, and saving a different Kokoro repo persists it while leaving `whisper_model` untouched in the same file — `bot-template/app/main_bot.py`, `bot-template/static_bot/admin.html`, `app/main.py`, `static/admin.html`, `techsupport-voice-bot/app/main_tech.py`, `techsupport-voice-bot/static_tech/admin.html`
- 2026-08-19 20:10 — Added a **Shut Down Platform** button to the Launcher's top bar (visible from the Overview and every other view), which runs `stop_all.bat` — the same script as the desktop shortcut, so there is one definition of "stop everything" rather than two that drift apart. It stops Meeting Intelligence, the Portal, AI Studio, every running bot, Ollama, and the Launcher itself, after a confirm that spells out exactly that. Two ordering hazards had to be handled, because this script is now started **by** the thing it kills. First, `taskkill /F /T` walks parent-PID chains, so a batch spawned normally by the Launcher would be killed along with it — it goes through the existing `_spawn_detached()` helper, already written for this exact problem, extended with a `window=True` option that drops its `/B` so the script gets a real console (its output and `pause` are the only thing left to look at once the server is gone). Second, `stop_all.bat` killed ports in ascending order, reaching the Launcher on 8004 **before** AI Studio on 8005 and Ollama on 11434 — so even a surviving script was one interruption away from leaving two services running. The list now ends on 8004 deliberately, commented as such, which is also the right order when the script is run by hand. The page does not pretend to still be live afterwards: it replaces itself with a "Platform stopping" state telling you to run `start_all.bat`, and a connection dropped mid-request is treated as the shutdown working rather than as an error. Verified without ever stopping the running platform, by intercepting the spawn: the endpoint returns 200, spawns `cmd /c <repo>\stop_all.bat` from the repo root with its own window, refuses unauthenticated callers with a 401, and checks the script exists first; and in the browser, cancelling the confirm posts nothing and leaves the button live, confirming posts and swaps in the stopping page, a dropped connection still shows it, and a genuine server-side refusal restores the button with the reason in a toast — `launcher/app/main_launcher.py`, `launcher/static_launcher/index.html`, `stop_all.bat`
- 2026-08-20 10:58 — Fixed **tapping the mic button ending the call**. Whisper never returns an empty transcript for silence: trained largely on captioned video, it emits the caption it expects to see, and its favourites — "Thank you.", "Thanks for watching." — are exactly the phrases the farewell detector reads as "the caller is finished". So a click-and-release with nothing said produced a goodbye and closed the session. `logs_tech/server.log` has it three times over, including **682 bytes of audio (21ms) transcribed as "Thank you for watching."** followed immediately by "Farewell detected — closing session". The existing `if not transcript` guard could never catch this, because the transcript was never empty. Audio now passes a cheap speech gate *before* Whisper is asked: under 400ms is rejected outright, and anything whose peak stays below 0.02 with an RMS below 0.004 is treated as silence. Both thresholds are deliberately lenient — the job is to reject taps and room tone, not to judge audio quality — and the turn ends with "I didn't catch anything — hold the button while you speak" rather than a farewell. As a second layer, a farewell that is *only* a known Whisper filler phrase and arrives as the caller's opening turn is treated as noise rather than a goodbye, since hanging up on it cannot be undone; a real "thanks, bye" later in the call, or "Thank you, that fixed it", still ends it normally. Verified against the exact captures from the log: the 682-byte tap and the 38230-byte quiet capture (peak 0.006, rms 0.0015) are both now ignored, as are digital silence and room tone, while normal, quiet and very quiet speech all still pass — and the gate is confirmed to sit ahead of `transcribe()` in the running module — `bot-template/app/main_bot.py`, `app/main.py`, `techsupport-voice-bot/app/main_tech.py`
- 2026-08-20 11:03 — Fixed the Launcher Overview's **AI Bots tile showing "Down" while a bot was running**. The tile was repainted with `element.outerHTML = statusTagHtml(...)`, which replaces the element — *including its `id`* — so the first paint worked and every one after it threw on a null lookup, taking the rest of `loadOverview()` with it. Start a bot and return to the Overview and the tile still showed whatever it said at first load. Status tags now repaint in place through a `setStatusTag()` helper that keeps the id, so the tile is Live whenever **any** bot is up (individual bot tiles keep reporting their own state, unchanged). The same broken pattern was repainting the Meeting Intelligence, Portal and AI Studio tiles, so those were stuck too and are fixed by the same change. Verified in the browser across repeated repaints: Down → Live → Down → Live with the id surviving every time — `launcher/static_launcher/index.html`
- 2026-08-20 11:03 — Stripped the **diagnostics/logging panel and the background-noise toggle off the customer-facing agent page** — those are operator controls and belong in the admin panel, not on the screen a caller sees. Out go the Session Log / Health / WAV Files / System tabs, the log box with its level filter and download buttons, the full diagnostics report, and the 🎧 BG Noise on/off button: 720 lines lighter, leaving Stop, Restart Chat, Stop Waiting and Mute output. Ambience still plays exactly as configured in the admin panel — it just can't be toggled by whoever is on the call. `logEntry()` stays as an in-memory ring (capped at 500 entries) since it is called from all over the page, but no longer writes to a DOM panel; the bot's own server log in the admin panel is the record worth reading. Also fixed the bug reported alongside it: **background noise never came back after Stop**. `startBackgroundNoise()` was only ever called from `unlockAudio()`, which returns early once the audio context is unlocked — so Stop paused the ambience and the next mic tap short-circuited before restarting it. `unlockAudio()` now restarts the ambience on every tap rather than only the first. Verified against the running bot: panel, log box and toggle all gone, no console errors, mic and transcript intact — `techsupport-voice-bot/static_tech/index.html`, `bot-template/static_bot/index.html`
- 2026-08-20 11:09 — Put the **platform ribbon on every page**: the same 🤖 mark and "AI CX Platform" wordmark, always a click back to the Launcher, with only the subheading after it changing to name the page you are on. Before this each page wore its own identity in its header — "Unified Ops Portal", "AI Studio Admin", "Meeting Intelligence Admin", "Tech Support Admin" — which read as four products rather than one, and the three customer-facing agent pages had **no way back to the Launcher at all**. The six pages that already had a header link keep it, with the platform mark and wordmark in front and their old title demoted to the subheading (`AI CX Platform / Meeting Intelligence`), so nothing was added to those layouts. The agent pages get a real fixed full-width bar, which also gave the floating theme toggle a home: it used to be `position: fixed` at the top-right and would have sat on top of the new ribbon, so it now rides inside it, right-aligned. Only the mark and wordmark are the link — the subheading is not clickable, since it names where you already are. On a bot page the subheading follows that bot's own branding rather than a hardcoded name, so a custom bot reads as itself the moment its name is saved. The Launcher itself is left as it was: it is the page everything else links back to, and its sidebar already carries the same mark and wordmark with a per-view title beside it. Verified live: the agent ribbon is pinned at y=0, full width, with the toggle right-aligned and clear of the wordmark, the page header below it and no horizontal scroll; the admin ribbon reads "AI CX Platform / Tech Support Bot — Admin" on one line and still points at :8004 — `portal/static_portal/dashboard.html`, `ai-studio/static_studio/admin.html`, `meeting-intelligence/static_meet/admin.html`, `static/admin.html`, `static/index.html`, `techsupport-voice-bot/static_tech/admin.html`, `techsupport-voice-bot/static_tech/index.html`, `bot-template/static_bot/admin.html`, `bot-template/static_bot/index.html`
- 2026-08-20 11:27 — When a turn carries no speech the bot now **asks for it again out loud**, instead of going silent. The speech gate added earlier stopped a mic tap ending the call, but it only wrote a line of status text to the screen — and on a voice call nobody is reading the screen, so the bot simply said nothing and the caller was left wondering whether it was broken. All three dead ends (audio too short or too quiet, Whisper returning nothing, an opening turn that was only filler) now speak a prompt through the normal TTS path, and the wording escalates over consecutive misses: "Sorry, I didn't catch that — could you say that again?", then "I still didn't get that. Could you speak a little closer to the mic?", then "I'm not picking up any sound — check that your microphone is on, then try again." — by the third miss the caller needs a hint about their microphone, not the same apology. The run resets the moment a turn carries real speech, and the lines are localised alongside the existing nudges so a Hindi bot asks in Hindi. Caught two bugs while building it: `missed_turns` is mutated inside `process_audio()`, a nested function, so without `nonlocal` the **first miss would have raised UnboundLocalError** — worse than the silence it replaced; and the pre-warmed English Kokoro pipeline was still constructed without a `repo_id`, so the TTS model chosen in the admin panel was ignored at startup for the default language (it only took effect once something cleared the pipeline cache). Verified by driving the real `/ws/voice` handler with 62ms of silence three times over: the log shows each miss ignored by the gate, the escalating line spoken, and TTS audio synthesised and sent for each one — `bot-template/app/main_bot.py`, `app/main.py`, `techsupport-voice-bot/app/main_tech.py`
